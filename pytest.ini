[pytest]
# Put the repo root on sys.path so `import app...` works regardless of how
# pytest is invoked (plain `pytest` doesn't add the cwd, unlike `python -m pytest`).
pythonpath = .
testpaths = tests
